#include <string>
#include <iostream>

using namespace std;

// On time on space 
// iterative approach
string reverse(string input){

  string output = "";
  for (int i = input.length(); i >=0; i--){
    output += input[i];
  }

  return output; 
  
}