#include <iostream>
#include <string>
using namespace std;


bool palindrone(string str){

  int left = 0;
  int right = str.length() - 1;

  while (right > left){
    if (str(l++) != str(r--)){
      return false; 
    }
  }
  return true; 
}


int main(){

  string str = "aba";
  string str1 = "abb";
  cout << palindrone(str) << "\n";
  cout << palindrone(str1) << "\n";

}
